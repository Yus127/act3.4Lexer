#include <stdio.h>
int foo(int bar) {
    /* do so
    mething */
    return bar * 2;
}

int main() {
  printf
    foo(1);
    //ad =
    1.3
    2e4
    ~ ! # $ %  ^ & * ( ) _ + | \ - = { } [ ] : ; < > ? , . /
}
"ab% -3"
